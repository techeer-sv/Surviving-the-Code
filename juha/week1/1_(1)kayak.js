// const fs = require("fs");
// const input = fs.readFileSync("/dev/stdin").toString().split(" ");

// // 입력
// const [N, S, R] = input[0].split(" ").map(Number);
// const damagedTeam = input[1].split(" ").map(Number);
// const extrakayakTeam = input[2].split(" ").map(Number);

// // 출력
// function solution(N, damagedTeam, extrakayakTeam) {}
